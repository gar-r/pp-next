{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "env": {
                "MONGODB_URI": "mongodb://ppnext:ppnext@localhost:27017/?authSource=ppnext"
            }
        }
    ]
}